import styled from 'styled-components';
import { Colors } from '../globalstyles';

export const FormTitle = styled.h2`
    text-align: center;
    margin-top: 0;
    font-weight: 800;
    color: ${Colors.dark};
`;
