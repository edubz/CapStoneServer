import styled from 'styled-components';
import { Colors } from '../globalstyles';

export const GalleryDate = styled.h4`
    margin: 0 2em;
    width: 15%;
    font-weight: 600;
    color: ${Colors.light};
`;
