import styled from 'styled-components';
import { Colors } from '../globalstyles';

export const RecordPromptContent = styled.div`
    background-color: ${Colors.mid};
    margin: auto;
    padding: 2em;
    width: 80%;
`;
